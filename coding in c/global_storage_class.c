#include<stdio.h>


    int sum;// as  it is declared as a global variable then the compiler will take this as a global storage and it will be initialized with 0

   int function()
   {
return 0;
   }
   
   
    int main()
    {
        printf("the sumt is: %d", sum);
    return 0;
    }